# Development transcripts

Full Claude Code conversation records of this app being built
(JSONL, gzipped). Session 1 is the original conversation up to the
fork; session 2 continues from it (snapshot at commit time — the tail
of the session postdates the snapshot).
